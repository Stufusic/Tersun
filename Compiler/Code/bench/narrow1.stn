// E1: non-recursive fn with let + if/else + return
fn g(n: int) -> int {
    let r = 0;
    if (n < 2) {
        r = n;
    } else {
        r = 100;
    }
    return r;
}
fn main() {
    println(g(0));
    println(g(5));
}
